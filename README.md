# Face-detection
Performs real-time face detection using OpenCV's Haar Cascade classifier. It loads a pre-trained XML model, captures video from the webcam, converts each frame to grayscale, and detects faces using `detectMultiScale()`. Detected faces are highlighted with red rectangles. 
